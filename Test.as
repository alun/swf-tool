package  {		import flash.display.MovieClip;	import flash.events.Event;	import flash.events.EventDispatcher;	import flash.display.Stage;	import com.oggi.Logger.Logger;			public class Test extends MovieClip {		private function handler(e:Event):void {			trace("Hello world");		}		public function Test() {			Logger.debug("Listeners setup");			if (stage) {				setupListeners(stage);			} else {				addEventListener(Event.ADDED_TO_STAGE, function(e:Event):void {	 				setupListeners(stage);				});			}		}		public function setupListeners(s:EventDispatcher) {			s.addEventListener(Event.ADDED_TO_STAGE, handler);			s.addEventListener(Event.REMOVED_FROM_STAGE, handler);		}	}	}